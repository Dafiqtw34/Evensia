<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.evensia">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".booking.row_data_booking"></activity>
        <activity android:name=".chat.Chat" />
        <activity android:name=".profile.Profile" />
        <activity android:name=".booking.Booking" />
        <activity android:name=".gedung.pesanan.pesan_gedung" />
        <activity android:name=".home.row_data_event" />
        <activity android:name=".gedung.detail.row_data_ulasan" />
        <activity android:name=".gedung.detail.review" />
        <activity android:name=".gedung.row_data_recycler" />
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".login.Login1" />
        <activity android:name=".register.MainActivity" />
        <activity android:name=".home.Home" />
    </application>

</manifest>